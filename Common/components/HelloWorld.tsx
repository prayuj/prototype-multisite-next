const HelloWorld = ({ name }) => {
    return (
        <div>
            <h1>Hello From <strong>{name}</strong></h1>
            <p>This is a common react component</p>
        </div>
    );
}

export default HelloWorld;